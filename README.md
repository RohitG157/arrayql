# ArrayQL

A lightweight, SQL-like query engine for JavaScript arrays.

ArrayQL allows you to perform database-style queries on arrays of objects using a clean, chainable API.

---

## ✨ Features (Current Version)

- Type-safe generics
- `where()` with comparison operators
- `select()` projection
- Lazy execution (`value()`)
- Zero dependencies
- Built with TypeScript

---

## 📦 Installation

```bash
npm install arrayql