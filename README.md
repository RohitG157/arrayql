# ArrayQL

A lightweight, SQL-like query engine for JavaScript arrays.

ArrayQL allows you to perform database-style queries on arrays of objects using a clean, chainable, and type-safe API.

---

## ✨ Features

- Type-safe generics
- `where()` filtering with comparison operators
- `select()` projection
- `orderBy()` sorting (ASC / DESC)
- `limit()` support
- `offset()` support
- Lazy execution via `.value()`
- Zero dependencies
- Built with TypeScript

---

## 📦 Installation

```bash
npm install arrayql